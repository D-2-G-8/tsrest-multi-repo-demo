# manager

Run dev server:
```bash
npm install
cp .env.example .env
npm run dev
```

Open Swagger UI:
- http://localhost:3001/docs

This service exposes internal endpoints for the BFF (`/internal/v1/...`).
