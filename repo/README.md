# repo

Run dev server:
```bash
npm install
cp .env.example .env
npm run dev
```

Open Swagger UI:
- http://localhost:3002/docs

This service exposes internal endpoints for the BFF (`/internal/v1/...`).
