# auth

Run dev server:
```bash
npm install
cp .env.example .env
npm run dev
```

Open Swagger UI:
- http://localhost:3004/docs

This service issues and validates tokens for the BFF (`/internal/v1/auth/...`).
