# @acme/contracts

This repo contains API contracts shared between services and consumers, written with **ts-rest** + **zod**.

Contracts included:
- `bffFrontendContract` — Frontend -> BFF
- `bffManagerContract` — BFF -> Manager service
- `bffRepoContract` — BFF -> Repo service

Build:
```bash
npm install
npm run build
```
