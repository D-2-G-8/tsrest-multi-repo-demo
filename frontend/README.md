# frontend (Next.js)

```bash
npm install
cp .env.local.example .env.local
npm run dev
```

Open:
- http://localhost:3003

Requires the BFF to be running on `NEXT_PUBLIC_BFF_URL` (default: http://localhost:3000).
